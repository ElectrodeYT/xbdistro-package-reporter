ca-certificates:
  action: ignore

## Managarm specific
managarm-kernel:
  action: ignore
managarm-system:
  action: ignore
mlibc:
  action: ignore
mlibc-headers:
  action: ignore
cxxshim:
  action: ignore
libasync:
  action: ignore
libsmarter:
  action: ignore
core-files:
  action: ignore
frigg:
  action: ignore
fafnir:
  action: ignore
lewis:
  action: ignore

## Meta-packages
weston-desktop:
  action: ignore
base:
  action: ignore
base-devel:
  action: ignore